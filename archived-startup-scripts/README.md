# Archived Startup Scripts

这些是旧启动入口，当前 Windows 部署不再直接使用。

当前只保留根目录两个 Windows 启动入口：

- `Start-Windows-TTS.ps1` / `Start-Windows-TTS.bat`
- `Start-Windows-Backend-Only.ps1` / `Start-Windows-Backend-Only.bat`

归档文件仅用于排查历史方案或临时回滚，不作为日常启动方式。
