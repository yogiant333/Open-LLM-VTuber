# Copy this file to .env and fill local deployment secrets.
# .env is ignored by git and must not be committed.

# Default LLM key used by conf.yaml:
DEEPSEEK_API_KEY=

# Exa MCP search server key used by mcp_servers.json:
EXA_API_KEY=
